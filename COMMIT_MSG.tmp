v0.9.0 docs: ADR amendments part 3 (sftp.args), ADR-007/008 discharge, limitations narrowed

- Version 0.9.0 (single-sourced), the 0.9.0 changelog entry, the
  installers' default version.
- ADR-002 amendment part 3: the sftp key_file/known_hosts mechanism
  with the restic-source evidence (native ssh exec.Command, sftp.args
  since 0.16.1, the tokenizer's quoting rules).
- ADR-007: the pid-reuse revisit condition discharged (start-time
  verification; the non-Linux-unix degradation recorded honestly).
- ADR-008: the sidecar image contract delivered (volume `image` field).
- limitations.md rewritten: five entries closed, the named-volume
  entry narrowed to the Desktop-VM gate, the non-Linux-unix lock
  degradation added.
- README/config.md/testing.md/template refreshed to the v0.9.0 truth.
